module.exports = {
  /*
   * Enforces parentheses around arrow function
   * parameters, for consistency and readability.
   */
  arrowParens: "always",
  /*
   * Controls whether to print spaces between brackets in object literals.
   */
  bracketSpacing: true,
  /*
   * Sets the end of line sequence.
   */
  endOfLine: "lf",
  /*
   * Defines how HTML tags with missing or blank content are treated.
   */
  htmlWhitespaceSensitivity: "css",
  /*
   * Controls whether a top-level pragma comment is inserted into the output.
   */
  insertPragma: false,
  /*
   * If true, puts the > of a multi-line JSX element at the end
   * of the last line instead of being alone on the next line.
   */
  jsxBracketSameLine: false,
  /*
   * Controls whether to use single or double quotes in JSX attributes.
   */
  jsxSingleQuote: false,
  /*
   * Sets the maximum length of a line of code
   * before it's wrapped to the next line.
   */
  printWidth: 80,
  /*
   * Defines how to wrap prose.
   */
  proseWrap: "preserve",
  /*
   * Configures the usage of quotes in object literal property names.
   */
  quoteProps: "as-needed",
  /*
   * Controls whether a top-level pragma comment is required in the output.
   */
  requirePragma: false,
  /*
   * Controls whether semicolons are used to terminate statements.
   */
  semi: false,
  /*
   * Configures whether to use single or double quotes for strings.
   */
  singleQuote: false,
  /*
   * Sets the number of spaces per indentation level.
   */
  tabWidth: 2,
  /*
   * Controls whether to print trailing commas wherever possible.
   */
  trailingComma: "none",
  /*
   * Controls whether to use tabs or spaces for indentation.
   */
  useTabs: false
}
